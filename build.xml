<?xml version="1.0" encoding="Shift_JIS" ?>
<project name="SBMLPlugin" default="" basedir=".">
  <description>
    SBMLPlugin build file
  </description>
  <property name="src" location="src" />
  <property name="build" location="bin" />
  <property name="dist"  location="dist" />
  <property name="pluginsDir"  location="$basedir/../../IJ/plugins/" />
  <property name="user.name" value="Kaito Ii" />
  <target name="main" depends="compress" description="Main target">
    <echo>
      Building the .jar file.
    </echo>
  </target>
  <target name="compress" depends="" description="generate the distribution">
    <jar jarfile="Spatial_SBML.jar">
      <fileset dir="${basedir}" includes="plugins.config" />
      <fileset dir="${build}" includes="*.class" />
      <manifest>
        <attribute name="Built-By" value="${user.name}"/>
      </manifest>
    </jar>
    <copy file="Spatial_SBML.jar" toDir="${pluginsDir}"/>
    <copy file="Spatial_SBML.jar" toDir="/Applications/Fiji.app/plugins" overwrite="true"/>
  </target>
</project>
